/**
 * Application Constants
 *
 * Centralized configuration for magic numbers and global constants.
 * This makes it easier to tweak application behavior without digging into the logic.
 */

// API configuration
export const API_TIMEOUT_MS = 8000;

// Cache configuration
export const CACHE_EXPIRATION_TIME_MS = 60 * 60 * 1000; // 1 hour

// UI configuration
export const TOAST_DURATION_MS = 2500;
